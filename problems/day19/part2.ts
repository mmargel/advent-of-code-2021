
import { runTest } from "../utils/runTest.js";

const findSolution = (_values: string[]) : number => {
  return -1;
};

export const solvePart = () =>
  runTest({ day: 19, part: 2, testMethod: findSolution, test: true });
